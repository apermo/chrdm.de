# Dependency directories
/vendor/

# WordPress core (installed via Composer)
/web/wp/

# Local development repos (cloned separately, symlinked)
/repos/

# Themes - ignore all, whitelist custom
/web/app/themes/*
!/web/app/themes/.gitkeep
# !/web/app/themes/my-custom-theme/

# Plugins - ignore all, whitelist custom
/web/app/plugins/*
!/web/app/plugins/.gitkeep
# !/web/app/plugins/my-custom-plugin/

# mu-plugins - ignore all, whitelist custom
/web/app/mu-plugins/*
!/web/app/mu-plugins/.gitkeep
!/web/app/mu-plugins/msls-deepl-translate.php

# Uploads
/web/app/uploads/*
!/web/app/uploads/.gitignore
!/web/app/uploads/.htaccess

# Languages (downloaded by inpsyde/wp-translation-downloader)
/web/app/languages/
# Lockfile tracks "already downloaded" state — but checking only the lockfile
# (not the file system) causes the downloader to silently skip in fresh CI
# checkouts where the lockfile is committed but languages/ is gitignored.
wp-translation-downloader.lock

# Environment files
.env
.env.*
!.env.example

# Logs
*.log
/web/app/debug.log

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# PHP
*.cache
*.tmp

# Composer
composer.phar

# Build artifacts
/dist/
/build/

# Deploy
.deploy-cache/

# Database dumps
*.sql
*.sql.gz
